# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity proxy-pool start -->
## ProxyPool

Generated by HoundOS against `cabcd96`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · pip · host: kubernetes · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 6 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
<details><summary>9 issues found and fixed during self-review</summary>

- dependency-missing-pin: The instrumentation imports opentelemetry-api, opentelemetry-exporter-otlp-proto-http, opentelemetry-instrumentation-flask, opentelemetry-instrumentation-redis, opentelemetry-sdk but they are not declared as dependencies (no Python manifest is in this changeset). Installing from the manifest and running the app would fail with ModuleNotFoundError. Listing the packages in the PR description is NOT declaring them.
- proxy-pool-http-availability (`proxypool/telemetry.py`): The availability, error-rate, latency-p95/p99, and request-rate SLIs all rely on the FlaskInstrumentor emitting http.server.request.duration. FlaskInstrumentor().instrument() (bare, not instrument_app) patches the Flask class at import time, but run.py calls setup_telemetry() only before importing Scheduler — if the Flask app is constructed inside a forked worker, _reinit_after_fork rebuilds providers but does NOT re-run the instrumentors, so the HTTP SLIs depend on the class patch having applied before app creation. This is documented as a risk, so noted as warning.
- qr-preserves-behavior (`proxypool/telemetry.py`): os.register_at_fork(after_in_child=_reinit_after_fork) rebuilds providers on EVERY fork process-wide, but the instrumentors are not re-activated in children. More importantly _providers_initialized is set in the parent, so a child that re-imports the module still sees the guard; the after-fork hook mitigates this but leaks a new provider each fork without shutting down the inherited one.
- qr-preserves-behavior (`proxypool/telemetry.py`): FlaskInstrumentor().instrument() and RedisInstrumentor().instrument() are wrapped in bare `except Exception: pass`, silently swallowing any instrumentation failure; a genuine setup error becomes invisible dead telemetry rather than surfacing.
- qr-preserves-behavior (`proxypool/telemetry.py`): The instrumentor setup wraps FlaskInstrumentor().instrument() and RedisInstrumentor().instrument() in bare `except Exception: pass`, silently swallowing any failure so instrumentation can be dropped without any signal, leaving the HTTP/Redis SLIs unmeasured with no indication of why.
- sli-not-implemented (`proxypool/telemetry.py`): proxy-pool-http-request-rate has no dedicated request-count instrument; the platform's http.server.request.duration histogram from FlaskInstrumentor yields a _count series that backs throughput, so this is covered — but only via the auto-instrumentation count, not an explicit measurement point in the diff.
- qr-preserves-behavior (`proxypool/telemetry.py`): The FlaskInstrumentor and RedisInstrumentor instrument() calls are wrapped in bare `except Exception: pass`, which silently swallows any instrumentation failure. This is acceptable defensively but hides real wiring failures; not a control-flow change to the app.
- qr-not-a-noop (`proxypool/telemetry.py`): setup_telemetry() builds and registers the providers/instrumentors in the PARENT process at import time in run.py, but ProxyPool forks its API server, Getter and Tester as separate multiprocessing.Process workers. FlaskInstrumentor().instrument() patches the Flask class globally in the parent — but the Flask app that actually serves requests is constructed inside the forked child, and while os.register_at_fork rebuilds providers, class-level instrumentation patches applied to the parent's imported Flask class are what the child inherits; however the deeper defect is that if the child re-imports/reconstructs framework objects the parent-side instrument() may not cover child-created state.
- qr-export-pipeline (`proxypool/telemetry.py`): The parent process builds a TracerProvider/MeterProvider with BatchSpanProcessor and PeriodicExportingMetricReader that is never shut down or flushed; in the child, _reinit_after_fork explicitly discards the inherited providers without shutdown, so any telemetry buffered in the parent before the fork is lost on exit.

</details>
**Outstanding (1):**
- ⛔ qr-export-pipeline (`proxypool/telemetry.py`:38): _shutdown_inherited_providers() calls shutdown() on the inherited TracerProvider/MeterProvider inside the child's os.register_at_fork(after_in_child) hook; BatchSpanProcessor.shutdown() joins/blocks on the inherited background export thread that no longer exists in the child, which can hang the child (the exact fork deadlock the code claims to avoid) or, on the MeterProvider, flush against a broken thread — providers rebuilt afterward but the shutdown on the inherited BatchSpanProcessor is precisely the unsafe post-fork operation. — _Do not call shutdown() on inherited fork-unsafe processors in the child; simply build and register fresh providers without touching the inherited BatchSpanProcessor/PeriodicExportingMetricReader._

## SLIs implemented
- **ProxyPool — HTTP Service Availability** (`proxy-pool-http-availability`) — target 99.95% over 30-day
- **ProxyPool — HTTP Response Time P95** (`proxy-pool-http-latency-p95`) — target < 150ms over 1-hour
- **ProxyPool — HTTP Response Time P99** (`proxy-pool-http-latency-p99`) — target < 750ms over 1-hour
- **ProxyPool — HTTP 5xx Error Rate** (`proxy-pool-http-error-rate`) — target < 0.1% over 1-hour
- **ProxyPool — HTTP Request Throughput** (`proxy-pool-http-request-rate`) — target baseline ± 30% over 5-minute
- **ProxyPool — Database Query Success Rate** (`proxy-pool-db-query-success`) — target 99.9% over 1-hour

## Modified files
- `run.py`
- `requirements.txt`

## New files
- `proxypool/telemetry.py`

## Dependencies
- `opentelemetry-api==1.27.0`
- `opentelemetry-sdk==1.27.0`
- `opentelemetry-exporter-otlp-proto-http==1.27.0`
- `opentelemetry-instrumentation-flask>=0.48b0`
- `opentelemetry-instrumentation-redis>=0.48b0`
- `opentelemetry-api>=1.44.0,<2.0.0`
- `opentelemetry-sdk>=1.44.0,<2.0.0`
- `opentelemetry-exporter-otlp-proto-http>=1.44.0,<2.0.0`
- `opentelemetry-instrumentation-flask>=0.65b0`
- `opentelemetry-instrumentation-redis>=0.65b0`
<!-- houndos:entity proxy-pool end -->
