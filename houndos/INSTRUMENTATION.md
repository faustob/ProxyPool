# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity proxy-discovery-validation-and-serving start -->
## Proxy Discovery, Validation and Serving

Generated by HoundOS against `cabcd96`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · pip · host: kubernetes · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⚠ passed with warnings.
<details><summary>3 issues found and fixed during self-review</summary>

- qr-export-pipeline (`run.py`): otel.py defines shutdown_telemetry() to flush the BatchSpanProcessor and PeriodicExportingMetricReader, but run.py never calls it. For this short-lived CLI process the buffered spans and metrics are never flushed before exit, so telemetry is lost.
- qr-not-a-noop (`run.py`): run.py imports get_tracer/get_meter from proxypool.utils.otel (which triggers the module's side-effect provider registration) but never imports the otel module for its shutdown/other symbols; the registration side-effect does fire via the from-import, so this is only a fragility note.
- qr-export-pipeline (`proxypool/utils/otel.py`): run.py is a short-lived CLI process that exits after Scheduler().run() completes, but neither the TracerProvider nor the MeterProvider is ever shut down/flushed. The BatchSpanProcessor and PeriodicExportingMetricReader buffer telemetry, so the flow metrics and span recorded in the finally block are lost on process exit.

</details>
**Outstanding (1):**
- ⚠ sli-not-implemented (`run.py`:41): The flow-abandonment-rate SLI has no distinct measurement point: flow.outcomes records only 'success'/'failure' outcomes in the finally block, and an abandoned flow (process killed mid-run, no terminal record) cannot be distinguished — no explicit abandonment signal is emitted. — _Record an explicit 'abandoned'/'started-but-not-terminal' dimension or a separate abandonment instrument so the abandonment-rate SLI has data behind it._

## SLIs implemented
- **Proxy Discovery, Validation and Serving — E2E Business Flow Success Rate** (`flow-proxy-lifecycle-e2e-flow-success`) — target 99.95% over 30-day
- **Proxy Discovery, Validation and Serving — E2E Business Flow Latency P95** (`flow-proxy-lifecycle-e2e-flow-latency`) — target < 500ms over 24-hour
- **Proxy Discovery, Validation and Serving — Flow Validation Failure Rate** (`flow-proxy-lifecycle-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Proxy Discovery, Validation and Serving — E2E Business Flow Throughput** (`flow-proxy-lifecycle-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Proxy Discovery, Validation and Serving — Flow Completion Freshness** (`flow-proxy-lifecycle-e2e-flow-freshness`) — target < 2 minutes over 1-hour
- **Proxy Discovery, Validation and Serving — Flow Abandonment Rate** (`flow-proxy-lifecycle-e2e-flow-abandonment-rate`) — target < 10% over 7-day

## Modified files
- `run.py`

## New files
- `proxypool/utils/otel.py`

## Dependencies
- `opentelemetry-api`
- `opentelemetry-sdk`
- `opentelemetry-exporter-otlp`
<!-- houndos:entity proxy-discovery-validation-and-serving end -->
