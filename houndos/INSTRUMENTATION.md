# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity proxy-discovery-validation-and-serving start -->
## Proxy Discovery, Validation and Serving

Generated by HoundOS against `cabcd96`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · pip · host: kubernetes · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⚠ passed with warnings.
<details><summary>6 issues found and fixed during self-review</summary>

- dependency-missing-pin: The instrumentation imports opentelemetry-api, opentelemetry-exporter-otlp-proto-grpc, opentelemetry-sdk but they are not declared as dependencies (no Python manifest is in this changeset). Installing from the manifest and running the app would fail with ModuleNotFoundError. Listing the packages in the PR description is NOT declaring them.
- qr-export-pipeline (`proxypool/telemetry.py`): The MeterProvider and TracerProvider are never shut down on process exit; run.py has no atexit/shutdown call, so buffered spans and metrics in the BatchSpanProcessor and PeriodicExportingMetricReader can be lost when the batch job exits.
- sli-not-implemented (`proxypool/telemetry.py`): The Flow Validation Failure Rate SLI has no measurement point: the diff never creates or records the flow.validation.outcomes instrument (present in managedMetricNames), and no validation step is instrumented anywhere in the diff.
- dependency-missing-pin: The instrumentation imports opentelemetry-api, opentelemetry-exporter-otlp-proto-grpc, opentelemetry-sdk but they are not declared as dependencies (no Python manifest is in this changeset). Installing from the manifest and running the app would fail with ModuleNotFoundError. Listing the packages in the PR description is NOT declaring them.
- sli-not-implemented (`proxypool/telemetry.py`): The flow.validation.outcomes counter (backing the Flow Validation Failure Rate SLI) is created but never recorded anywhere: neither run.py nor any other file in the diff calls flow_validation_outcomes_counter.add. The validation-failure-rate SLI has no measurement point and reads as healthy forever.
- qr-unused-instrument (`proxypool/telemetry.py`): flow_validation_outcomes_counter is a newly exported instrument that no code path records to, so it emits nothing.

</details>
**Outstanding (1):**
- ⚠ sli-not-implemented (`run.py`): The abandonment-rate SLI (flow-proxy-lifecycle-e2e-flow-abandonment-rate) has no dedicated measurement point; the flow always records a terminal outcome (success/failed) in the finally block and there is no instrument distinguishing an abandoned/incomplete flow from a completed one. — _Record an abandonment signal (e.g. an 'abandoned' outcome value or a separate counter) for flows that terminate without reaching a terminal state, so the abandonment-rate SLI has data behind it._

## SLIs implemented
- **Proxy Discovery, Validation and Serving — E2E Business Flow Success Rate** (`flow-proxy-lifecycle-e2e-flow-success`) — target 99.95% over 30-day
- **Proxy Discovery, Validation and Serving — E2E Business Flow Latency P95** (`flow-proxy-lifecycle-e2e-flow-latency`) — target < 500ms over 24-hour
- **Proxy Discovery, Validation and Serving — Flow Validation Failure Rate** (`flow-proxy-lifecycle-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Proxy Discovery, Validation and Serving — E2E Business Flow Throughput** (`flow-proxy-lifecycle-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Proxy Discovery, Validation and Serving — Flow Completion Freshness** (`flow-proxy-lifecycle-e2e-flow-freshness`) — target < 2 minutes over 1-hour
- **Proxy Discovery, Validation and Serving — Flow Abandonment Rate** (`flow-proxy-lifecycle-e2e-flow-abandonment-rate`) — target < 10% over 7-day

## Modified files
- `run.py`

## New files
- `proxypool/telemetry.py`
- `requirements-otel.txt`

## Dependencies
- `opentelemetry-api==1.44.0`
- `opentelemetry-sdk==1.44.0`
- `opentelemetry-exporter-otlp==1.44.0`
- `opentelemetry-api>=1.44.0,<2.0.0`
- `opentelemetry-sdk>=1.44.0,<2.0.0`
- `opentelemetry-exporter-otlp-proto-grpc>=1.44.0,<2.0.0`
<!-- houndos:entity proxy-discovery-validation-and-serving end -->
