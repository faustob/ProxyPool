# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity proxy-pool start -->
## ProxyPool

Generated by HoundOS against `cabcd96`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · pip · host: kubernetes · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ✓ clean — no issues found.
<details><summary>4 issues found and fixed during self-review</summary>

- qr-otel-version-pin (`requirements.txt`): opentelemetry-exporter-otlp is pinned at 1.27.0, but this job targets the OTel 1.44 line (default-latest). Different PRs against one repo pinning different OTel lines makes the dependency move for no stated reason.
- dependency-auto-added (`requirements.txt`): Added opentelemetry-exporter-otlp-proto-grpc>=1.44.0,<2.0.0 to requirements.txt — imported by the instrumentation but not declared.
- sli-not-implemented (`proxypool/processors/server.py`): The HTTP availability, P95/P99 latency, 5xx error-rate, and request-throughput SLIs rely on http.server.request.duration and related server metrics, but no such histogram/counter is created or recorded anywhere in the diff. The only HTTP instrumentation added is FlaskInstrumentor().instrument_app(app), which produces spans, not the managed server request metrics that back these SLIs.
- sli-not-implemented (`proxypool/processors/server.py`): proxy-pool-http-latency-p95, p99, error-rate, availability and request-rate SLIs have no metric measurement point; the after_request hook only adds a span event ('slow_request') and FlaskInstrumentor emits spans, neither of which is a metric backing these SLIs.

</details>

## SLIs implemented
- **ProxyPool — HTTP Service Availability** (`proxy-pool-http-availability`) — target 99.95% over 30-day
- **ProxyPool — HTTP Response Time P95** (`proxy-pool-http-latency-p95`) — target < 150ms over 1-hour
- **ProxyPool — HTTP Response Time P99** (`proxy-pool-http-latency-p99`) — target < 750ms over 1-hour
- **ProxyPool — HTTP 5xx Error Rate** (`proxy-pool-http-error-rate`) — target < 0.1% over 1-hour
- **ProxyPool — HTTP Request Throughput** (`proxy-pool-http-request-rate`) — target baseline ± 30% over 5-minute
- **ProxyPool — Database Query Success Rate** (`proxy-pool-db-query-success`) — target 99.9% over 1-hour

## Modified files
- `proxypool/setting.py`
- `proxypool/testers/__init__.py`
- `proxypool/crawlers/__init__.py`
- `proxypool/crawlers/base.py`
- `proxypool/crawlers/public/zhandaye.py`
- `proxypool/crawlers/public/seofangfa.py`
- `proxypool/utils/proxy.py`
- `examples/usage2.py`
- `proxypool/storages/redis.py`
- `proxypool/processors/server.py`
- `proxypool/scheduler.py`
- `requirements.txt`

## New files
- `proxypool/telemetry.py`

## Dependencies
- `opentelemetry-api>=1.44.0,<2.0.0`
- `opentelemetry-sdk>=1.44.0,<2.0.0`
- `opentelemetry-exporter-otlp>=1.44.0,<2.0.0`
- `opentelemetry-exporter-otlp-proto-grpc>=1.44.0,<2.0.0`
- `opentelemetry-instrumentation-flask>=0.65b0,<1.0.0`
<!-- houndos:entity proxy-pool end -->
