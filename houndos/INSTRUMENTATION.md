# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity proxy-pool start -->
## ProxyPool

Generated by HoundOS against `cabcd96`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** python · pip · host: kubernetes · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 6 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
<details><summary>8 issues found and fixed during self-review</summary>

- qr-preserves-behavior (`proxypool/telemetry.py`): The response hook reads span duration from span.start_time and span.attributes, but the Flask instrumentor's server span is a live Span object that does not reliably expose these as plain attributes on the running span; the slow-request event will typically never fire. This is instrumentation logic that silently does nothing rather than a behavior change of the app.
- qr-unwired-telemetry (`proxypool/telemetry.py`): The telemetry module is only imported from run.py, which just parses --processor and runs the Scheduler. If the Flask Server process is started by Supervisor via a different entrypoint (e.g. the scheduler subprocess or a WSGI entry) that does not import proxypool.telemetry, FlaskInstrumentor().instrument() never runs and the HTTP SLIs emit nothing.
- qr-metric-naming (`proxypool/telemetry.py`): The new counter name "http.server.request.outcome" with unit "1" is a plain count and should be unsuffixed per semconv style; the tail segment "outcome" also reads as a dimension (already carried as an attribute) rather than a measurement noun.
- qr-preserves-behavior (`proxypool/telemetry.py`): The Flask got_request_exception handler _handle_request_exception is registered but the FlaskInstrumentor already sets span status/error attributes; this only adds attributes and returns None, so it does not alter Flask's error response and is behavior-preserving — noting only that the OTLP span exporter/reader are never shut down (no atexit/shutdown), so buffered spans and metrics can be lost on process exit.
- general (`proxypool/telemetry.py`): The line `FlaskInstrumentor().instrument(...)so so this so (no-op guard not needed; instrument() is idempotent-safe per library) below.` contains garbled non-Python text that is a syntax error and will fail to import.
- qr-preserves-behavior (`proxypool/telemetry.py`): FlaskInstrumentor().instrument(...) is called twice with hooks, which registers the request/response hooks twice and would cause the outcome counter to be incremented twice per completed request.
- sli-not-implemented (`proxypool/telemetry.py`): The SLIs proxy-pool-http-latency-p95 and proxy-pool-http-latency-p99 depend on the http.server.request.duration histogram from Flask auto-instrumentation, but the second (corrupted) instrument() line will crash import, and the p99 slow-path uses only a span event rather than a duration measurement point.
- qr-unwired-telemetry (`proxypool/telemetry.py`): telemetry.py is imported only from run.py, but Flask serving typically happens in the child process spawned by the scheduler/Supervisor (proxypool.processors.server), not in run.py's __main__ import. If the Server process is launched such that run.py's top-level import does not execute in that worker, FlaskInstrumentor and the providers never register in the serving process and no HTTP/Redis telemetry is emitted.

</details>
**Outstanding (1):**
- ⛔ sli-not-implemented (`proxypool/telemetry.py`): The db-query-success SLI (proxy-pool-db-query-success) measures Redis operation success, but nothing in the diff instruments any Redis call — no db.client.operation.duration instrument, no Redis client wrapping, and no instrumentor for redis. The SLI has no measurement point anywhere in the changeset. — _Add RedisInstrumentor().instrument() from opentelemetry-instrumentation-redis (and add it to the manifest), or record db.client.operation.duration around the Redis calls in the RedisClient._

## SLIs implemented
- **ProxyPool — HTTP Service Availability** (`proxy-pool-http-availability`) — target 99.95% over 30-day
- **ProxyPool — HTTP Response Time P95** (`proxy-pool-http-latency-p95`) — target < 150ms over 1-hour
- **ProxyPool — HTTP Response Time P99** (`proxy-pool-http-latency-p99`) — target < 750ms over 1-hour
- **ProxyPool — HTTP 5xx Error Rate** (`proxy-pool-http-error-rate`) — target < 0.1% over 1-hour
- **ProxyPool — HTTP Request Throughput** (`proxy-pool-http-request-rate`) — target baseline ± 30% over 5-minute
- **ProxyPool — Database Query Success Rate** (`proxy-pool-db-query-success`) — target 99.9% over 1-hour

## Modified files
- `run.py`

## New files
- `proxypool/telemetry.py`

## Dependencies
- `opentelemetry-api==1.44.0`
- `opentelemetry-sdk==1.44.0`
- `opentelemetry-exporter-otlp-proto-http==1.44.0`
- `opentelemetry-instrumentation-flask`
<!-- houndos:entity proxy-pool end -->
