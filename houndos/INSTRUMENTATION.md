# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity user-defined-proxy-pool start -->
## Instrumentation for User-defined — ProxyPool

Generated by HoundOS for the agreed SLO contract (v2). This PR adds the
OpenTelemetry instrumentation needed to emit the SLIs selected below.

## SLIs implemented
- **Proxy fetch success rate** (`custom-user-defined-proxypool-proxy-fetch-success-rate`) — target 99% over 30-day

## Dependencies
No additional packages required beyond your existing OpenTelemetry SDK.

## Files
- `houndos.yaml` — the agreed SLO contract (source of truth).
- Renamed from `telemetryos.yaml` / `telemetryos/` if this repo still had them — TelemetryOS is now HoundOS, and the contract moves with the name.
<!-- houndos:entity user-defined-proxy-pool end -->
